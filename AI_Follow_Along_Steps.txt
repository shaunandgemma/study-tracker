Create the Follow Along using this exact controlled process.

1. Work only inside:

E:\code\study-tracker

2. Do not change an existing handoff package or builder.

3. Create a new uniquely named folder under:

E:\code\study-tracker\docs\author-assistant\handoffs\

Use this naming format:

author-assistant-[service-or-topic]-codex-[date]-001

4. Use this verified builder only as the structural reference:

E:\code\study-tracker\docs\author-assistant\handoffs\author-assistant-terraform-variables-validation-codex-20260815-001\build-terraform-variables-validation-handoff.mjs

Copy its structure, but create completely new IDs, content, filenames and fingerprints.

5. Do not invent a simplified JSON schema. Preserve the complete structures for:

- schema
- programme
- sources
- presentation
- storage
- progress
- capabilities
- phases
- tasks
- resources
- warnings
- cleanup
- extensions
- review
- publication
- identityBinding
- summary
- handoffBoundary

6. Give the programme a unique ID ending with:

-learning-path

7. Every phase and task must have a unique stable ID.

8. Write complete beginner instructions. Assume:

- No infrastructure already exists.
- The learner may not know how to create folders or files.
- Every important click, field, value and command must be explained.
- Explain what is happening and why it matters.
- Never use placeholder, deferred or “add later” content.

9. Include complete learning routes:

- AWS Management Console or graphical verification where relevant
- Terraform and AWS CLI commands
- Visible expected results
- Verification checks
- Supplied configuration or JSON blocks where required

10. Use only official documentation:

- AWS documentation from docs.aws.amazon.com
- Terraform documentation from developer.hashicorp.com

Every source must link both ways:

- Source lists the tasks using it.
- Each task lists its source IDs.

11. Keep configuration code in separately visible editable blocks. Use:

- `language: "json"` only for valid JSON
- `language: "text"` for Terraform, YAML, shell scripts and other non-JSON content

12. Every task must include:

- Title
- Feature
- Goal
- Why it matters
- Difficulty of Easy, Medium or Hard
- Phase ID
- Prerequisites
- Mode availability
- Source IDs
- Complete Console instructions
- Complete CLI commands
- Expected results
- At least one verification check

13. Cleanup must:

- Be manual
- Use reverse-dependency order
- Name every exact resource
- Destroy cloud resources before credentials, state or local files
- Include visible verification
- Finish with a complete programme-cleanup acknowledgement

Use:

- `completionGate: "acknowledgement"`
- `manualOnly: true`
- `ordering: "reverse_dependency"`

14. Warnings must be an object containing:

- cost
- safety
- credentials
- region

15. Publication must remain:

- `publishStatus: "not_published"`
- No candidate
- No approval
- No publication

16. Preserve these boundaries:

- No Author identity binding
- No Author write
- No Supabase write
- No AWS connection
- No release candidate
- No approval
- No publishing
- Stage 12 not started

17. Validate the finished content with:

- `validateAuthorPlanning`
- `validateAuthorContent`
- `validateAuthorReview`

Do not continue if any validator reports an error.

18. Generate these files first:

- author-local-handoff-package.json
- session.json
- complete-follow-along-preview.txt

19. Calculate the handoff fingerprint using the existing app fingerprint helper. Do not manually invent the hash.

20. Show the user:

- Programme name
- Phase count
- Task count
- Checkbox count
- CLI command count
- Verification count
- Cleanup count
- Source count
- Exact SHA-256 handoff fingerprint
- Full folder location

21. Stop and request exact human approval of that fingerprint.

22. Only after the user approves the exact fingerprint, create:

author-local-handoff-acceptance-90a.json

Use:

buildStage90ALocalAcceptance({ session, handoffPackage })

23. Verify the package and acceptance together using:

- `verifyStage90AAcceptance`
- `validateAuthorHandoffImportPreview`

24. Stop again. Do not import the package into Author unless the user separately approves the import.

25. Never commit, push, import, create a candidate, approve or publish without the user explicitly requesting that separate action.