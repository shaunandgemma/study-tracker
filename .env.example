VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Browser-safe switch only. Keep false until the approved Stripe sandbox activation step.
VITE_STRIPE_SANDBOX_PAYMENTS_ENABLED=false

# Browser-safe live switch only. Keep false until the final approved live cutover.
# Sandbox and live must never both be true; the browser fails closed if they are.
VITE_STRIPE_LIVE_PAYMENTS_ENABLED=false
