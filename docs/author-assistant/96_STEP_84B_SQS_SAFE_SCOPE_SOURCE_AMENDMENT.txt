STEP 84B - SQS SAFE-SCOPE SOURCE AMENDMENT
==========================================

1. APPROVED SCOPE

Step 84B was approved to:

1. Accept only the two displayed official AWS sources.
2. Link them only to the SQS safe-scope/access task.
3. Preserve the five completed Stage 6 tasks exactly.
4. Regenerate only task 1 as short editable checkboxes.
5. Never create or recommend an IAM policy.
6. Keep all output local and stop before Stage 7.

2. APPROVED SOURCES

1. Choosing your Region - AWS Management Console
   https://docs.aws.amazon.com/awsconsolehelpdocs/latest/gsg/select-region.html

2. Using policies with Amazon SQS
   https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-using-identity-based-policies.html

No third source is allowed by the Step 84B amendment.

3. TASK BOUNDARY

The two sources can be linked only to:

task-sqs-confirm-safe-scope-and-required-access-001

The AI request contains only that task. It cannot regenerate the other five
Stage 6 tasks.

4. REQUIRED CHECKBOX PURPOSE

The regenerated task must provide short Console prerequisite checks for:

1. Selecting eu-west-2.
2. Using a non-confidential queue name.
3. Confirming that the signed-in account or role already has the required SQS
   Console access.

The learner is directed to their administrator when access has not already
been granted.

5. IAM SAFETY RULE

Step 84B does not create, write, add, attach, edit, generate or recommend an
IAM policy.

A generated answer that tries to do any of those things is rejected before
the saved files are changed.

6. PRESERVATION RULES

The save is rejected if it attempts to:

1. Add a source outside the two approved URLs.
2. Link either new source to any other task.
3. Change an existing accepted source.
4. Change an unaffected blueprint task.
5. Change one of the five completed Stage 6 task records.
6. Write to Author, Supabase or AWS.
7. Begin Stage 7.

7. LOCAL AUDIT

After a successful secure live run, the following audit file will be saved in
the private SQS session folder:

author-stage-6-source-amendment-84b.json

It records:

1. The two accepted URLs.
2. The single linked task.
3. The five protected tasks.
4. That nothing was written to Author.
5. That Stage 7 was not begun.

8. FAILURE SAFETY

The accepted-source, blueprint, instruction, audit and session files are
prepared together. A failed AI request does not apply the source amendment or
replace the saved Stage 6 instructions.

9. FILES ADDED OR UPDATED

scripts/author-assistant/alignments/stage84b-sqs-safe-scope-sources.json
scripts/author-assistant/authorAssistantStage84B.mjs
scripts/author-assistant/authorAssistantInstructions.mjs
scripts/author-assistant/authorAssistantCore.mjs
scripts/author-assistant/authorAssistant.mjs
tests/authorAssistantStage84B.test.js

10. VERIFICATION

Focused Steps 84, 84A and 84B tests:
23 passed, 0 failed.

Full application tests:
589 passed, 0 failed.

Lint:
0 errors. Existing unrelated warnings remain.

Production build:
Passed. Existing Node-version and large-bundle warnings remain.

11. CURRENT LIVE STATUS

The Step 84B implementation is ready for the secure live run.

The user's real SQS session has not been changed during implementation or
testing because the one-task AI request requires the user's private OpenAI API
key.

Nothing was written to Author, Supabase or AWS.
Stage 7 was not started.

