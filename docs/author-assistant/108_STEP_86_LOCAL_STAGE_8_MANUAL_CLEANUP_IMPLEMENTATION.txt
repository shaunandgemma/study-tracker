STEP 86 - LOCAL STAGE 8 MANUAL CLEANUP IMPLEMENTATION

1. APPROVED PURPOSE

Prepare only local Author Stage 8 manual cleanup from the accepted Stages 1-7 package and accepted task-linked AWS Console documentation.

Do not connect to Author, AWS or Supabase.
Do not begin Stage 9.

2. COMMAND CREATED

Run from E:\code\study-tracker:

npm run author-assistant:stage-86:secure

The API key is hidden and used for this run only.
It is not saved in the repository, app or private session files.

3. EXACT CLEANUP BOUNDARY

The only approved AWS service resource that Stage 8 may delete is:

Resource type: Amazon SQS queue
Resource name: sqs-beginner-test
Cleanup task: task-sqs-review-queue-deletion-effects-006

The generator rejects instructions that attempt to delete:

1. all queues;
2. multiple queues;
3. other queues;
4. IAM resources;
5. messages as a separate cleanup operation; or
6. any other AWS resource.

4. REQUIRED LOCAL CONTENT

Stage 8 must contain:

1. two to four short separately editable manual task cleanup steps;
2. exactly one step that manually deletes sqs-beginner-test;
3. the exact queue name in every task cleanup step;
4. visible verification that sqs-beginner-test is no longer listed after refresh; and
5. one non-destructive final programme cleanup acknowledgement.

5. SOURCE BOUNDARY

Only accepted, task-linked, protected AWS Console documentation may be used.

AWS CLI reference pages are excluded from current cleanup instructions.

If protected evidence is missing, the script may perform one targeted retry for only the exact missing accepted URL.

If the retry fails, the exact missing URL is displayed and no Stage 8 file is saved.

6. CLI BOUNDARY

1. No CLI command is created.
2. Future CLI cleanup guidance remains separate.
3. Existing future CLI guidance is preserved exactly from accepted Stage 7.

7. FILES CREATED AFTER A SUCCESSFUL USER RUN

The files will be saved in the existing private SQS session folder:

C:\Users\shaun\AppData\Local\StudyTracker\AuthorAssistant\author-assistant-sqs-7861d0c2-8a53-46e5-b028-82160872758c\author-stage-8-cleanup.json

C:\Users\shaun\AppData\Local\StudyTracker\AuthorAssistant\author-assistant-sqs-7861d0c2-8a53-46e5-b028-82160872758c\author-stage-8-cleanup.txt

The JSON file contains the complete local review package.
The TXT file contains the short review preview.

8. STOP BOUNDARY

After preparation:

1. Stage 8 waits for human review.
2. Stage 8 is not accepted automatically.
3. Stage 9 remains unprepared.
4. Nothing is written to Author.
5. Nothing connects to AWS.
6. Nothing connects to Supabase.
7. No live queue is deleted by the script.
8. No candidate is prepared or published.

9. LIVE SQS PREFLIGHT

The live SQS session passed the Step 86 preflight.

1. Accepted Stages 1-7 fingerprints verified: yes.
2. Only approved target is sqs-beginner-test: yes.
3. Stage 8 already exists: no.
4. The preflight was answered with no.
5. No AI request was made.
6. No private session file changed.

10. TEST RESULTS

1. New Step 86 tests: 9 passed, 0 failed.
2. Full automated test suite: 631 passed, 0 failed.
3. Linter: 0 new errors.
4. Production build: passed.
5. Existing unrelated lint, Node-version and bundle-size warnings remain.

11. FILES ADDED OR UPDATED

1. scripts\author-assistant\authorAssistantCleanup.mjs
2. scripts\author-assistant\prepareStage86.mjs
3. scripts\author-assistant\startStage86.ps1
4. scripts\author-assistant\authorAssistantCore.mjs
5. tests\authorAssistantStage86.test.js
6. package.json
7. docs\author-assistant\108_STEP_86_LOCAL_STAGE_8_MANUAL_CLEANUP_IMPLEMENTATION.txt

12. CURRENT POSITION

Step 86 is implemented and tested.

No live Stage 8 cleanup package has been created yet.
Stage 9 has not started.

Run:

npm run author-assistant:stage-86:secure

Enter the private API key and answer y.
