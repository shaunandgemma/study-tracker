STEP 85A - PROTECTED CONSOLE SOURCE CORRECTION

1. APPROVED CORRECTION

Correct Stage 7 protected AWS source selection.

Keep future CLI guidance separate and unchanged.
Allow only one targeted protected evidence retry.
Show the exact missing source if the retry still fails.
Do not write to Author, AWS or Supabase.
Do not begin Stage 8.

2. CAUSE OF THE SAFE FAILURE

The accepted SQS package contains 11 AWS documentation sources.

1. Six are suitable for the current Console path.
2. Five are AWS CLI reference pages.

The first Step 85 implementation allowed the AI to cite any of the 11 accepted sources.
The AI cited a source that was not returned as protected evidence in that request.
The validator correctly stopped before saving Stage 7.

3. LIVE SQS SOURCE REVIEW

The corrected Stage 7 boundary contains these six accepted, task-linked sources:

1. Creating an Amazon SQS standard queue.
2. Receiving and deleting an Amazon SQS message.
3. Deleting an Amazon SQS queue.
4. Amazon SQS service authorization reference.
5. AWS Management Console Region selection.
6. Using policies with Amazon SQS.

All five AWS CLI reference pages are excluded from current Stage 7 Console content.
Every SQS task still has at least one eligible Console source.

4. CONSOLE AND CLI SEPARATION

Current Stage 7 checks may use only the six eligible Console sources.

Future CLI guidance remains preserved exactly as it was in accepted Stage 6.
No CLI command is generated.
No future CLI guidance is promoted into the current Console path.

5. ONE TARGETED RETRY

After the first protected request:

1. The script identifies only the eligible source URLs cited by the proposed Stage 7 content.
2. It checks which cited URLs were returned as protected evidence.
3. If any are missing, it performs one targeted protected request for only those exact URLs.
4. It combines valid evidence from the first request and the retry.
5. It does not perform a second retry.

6. CLEAR FAILURE MESSAGE

If an eligible source is still missing after the single retry, the script stops and displays the exact missing URL.

No Stage 7 file is saved after that failure.

7. UNCHANGED SAFETY BOUNDARIES

1. The accepted Stages 1-6 files are not changed.
2. Their fingerprints must still match before research starts.
3. Nothing connects to AWS.
4. Nothing connects to Supabase.
5. Nothing is written to Author.
6. Stage 7 is not accepted automatically.
7. Stage 8 cleanup is not prepared.
8. No candidate is prepared or published.

8. TEST RESULTS

1. Step 85 and 85A tests: 12 passed, 0 failed.
2. Full automated test suite: 614 passed, 0 failed.
3. Linter: 0 new errors.
4. Production build: passed.
5. Existing unrelated lint, Node-version and bundle-size warnings remain.

9. FILES UPDATED

1. scripts\author-assistant\authorAssistantResourcesChecks.mjs
2. tests\authorAssistantStage85.test.js
3. docs\author-assistant\104_STEP_85A_PROTECTED_CONSOLE_SOURCE_CORRECTION.txt

10. RUN THE CORRECTED STEP

From E:\code\study-tracker run:

npm run author-assistant:stage-85:secure

Enter the private API key and answer y when asked to prepare local Stage 7 Resources and Checks.

11. CURRENT POSITION

Step 85A is implemented and tested.
No Stage 7 content has been saved by Codex.
Stage 8 has not started.
