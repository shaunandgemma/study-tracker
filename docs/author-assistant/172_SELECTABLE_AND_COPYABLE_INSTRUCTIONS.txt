STEP 172 - SELECTABLE INSTRUCTIONS

1. Instruction wording can now be highlighted normally with the mouse.

2. Highlighting text does not tick or untick the learner checkbox.

3. No Copy button is added to individual instruction rows.

4. The separate JSON Copy JSON button remains available for structured JSON content.

5. The checkbox remains a separate control for learner progress.

6. This is a display-only improvement. It does not change drafts, candidates,
approvals, publishing, Supabase data or AWS resources.
