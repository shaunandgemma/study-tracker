168. FREER AI CONTENT COUNTS

1. REQUEST

Allow the Author Assistant to use however many detailed steps are genuinely needed, while requiring at least four phases and at least three tasks.


2. ACTIVE AUTHOR ASSISTANT RULES

1. Console steps per task: at least 1, no fixed maximum.
2. Checkbox instructions per Console step: at least 1, no fixed maximum.
3. CLI steps per task: at least 1, no fixed maximum.
4. Verification checks per task: at least 1, no fixed maximum.
5. Task cleanup steps: no fixed maximum.
6. Final programme cleanup steps: at least 1, no fixed maximum.
7. Phases: at least 4, no fixed maximum.
8. Tasks: at least 3, no fixed maximum.


3. AI INSTRUCTION

The AI is explicitly told to use however many Console steps, checkbox instructions, CLI steps, verification checks and cleanup steps the learner journey genuinely requires.


4. LEGACY PATH ALIGNMENT

1. The legacy blueprint now also requires at least four phases.
2. The legacy Stage 8 cleanup no longer has a maximum of four manual cleanup steps.
3. Its single-target and non-destructive cleanup safeguards remain active.


5. SAFETY RULES PRESERVED

1. Official AWS documentation restrictions remain active.
2. Credential-like content remains blocked.
3. Invented IAM policies remain prohibited.
4. Placeholder and deferred content remains blocked.
5. Every task must still contain Console and CLI guidance.
6. CLI command chaining remains blocked.
7. Cleanup remains manual and narrowly scoped.
8. No publishing or external write capability was added.


6. VERIFICATION RESULTS

1. Detailed test content containing ten Console steps, ten checkboxes, ten CLI steps, ten verification checks, ten task cleanup steps and ten final cleanup steps was accepted by the count rules.
2. Full automated test suite: 690 passed, 0 failed.
3. Production build: passed.
4. Linter: passed with no errors. Existing unrelated warnings remain.
5. Change-format check: passed. Existing Windows line-ending notices remain.


7. BOUNDARY

Nothing was written to Author, Supabase or AWS.

No draft, candidate, approval or publication was created.

Very large generations are still naturally constrained by the AI model's response capacity and the browser's protected file-size check.
