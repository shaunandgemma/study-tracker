STEP 84A - SQS CONSOLE BOUNDARY CORRECTION
===========================================

1. APPROVED SCOPE

Step 84A was approved to correct only the local SQS Stage 6 planning and
instruction boundary.

The correction must:

1. Remove receipt-handle requirements from AWS Console instructions.
2. Preserve receipt-handle guidance for a future AWS CLI path.
3. Keep queue deletion in Stage 8 cleanup.
4. Present missing official AWS sources for human approval.
5. Consolidate repeated review findings without changing their meaning.
6. Remain local and stop before Stage 7.

2. IMPLEMENTED BOUNDARY

An approved SQS-only alignment record now controls the three affected tasks:

1. Receive and inspect the test message.
2. Delete the received test message.
3. Review queue deletion effects.

The two already prepared tasks are outside the affected list and must be
preserved exactly during the revision.

3. CONSOLE AND CLI SEPARATION

AWS Console path:

1. Poll, select and inspect the test message in the Console.
2. Select and delete the retrieved message in the Console.
3. Do not ask the learner to copy or enter a receipt handle.

Future AWS CLI path:

1. Retain ReceiptHandle returned by receive-message.
2. Use the most recently returned ReceiptHandle with delete-message.
3. No CLI commands are generated by Step 84A.

4. CLEANUP SEPARATION

Stage 6 may explain that deleting a queue is permanent.

Stage 6 must not tell the learner to delete the queue.

Actual manual queue deletion remains reserved for Stage 8 cleanup.

5. REVIEW-FINDING CONSOLIDATION

The repeated findings are replaced by seven approved canonical findings that
preserve the existing meaning:

1. Region-selection source is still pending.
2. An exact least-privilege policy must not be inferred.
3. Standard-queue delivery limitations remain clear.
4. A retrieved Console message must be deleted after verification.
5. FIFO-only requirements remain outside this programme.
6. Receipt handles are future CLI scope only.
7. Queue deletion remains Stage 8 cleanup.

6. OFFICIAL AWS SOURCE CANDIDATES

The following sources were identified but were not accepted, linked to the
blueprint or used to generate instructions:

1. Choosing your Region - AWS Management Console
   https://docs.aws.amazon.com/awsconsolehelpdocs/latest/gsg/select-region.html
   Purpose: exact Console Region-selection guidance for eu-west-2.
   Status: pending human approval.

2. Using policies with Amazon SQS
   https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-using-identity-based-policies.html
   Purpose: explain SQS Console permission requirements without inventing an
   exact learner policy.
   Status: pending human approval.

7. LIVE REVISION BEHAVIOUR

The secure Author Assistant now detects the existing SQS Stage 6 review and
offers the approved Step 84A correction.

When approved at the prompt, it will:

1. Use the private API key for that run only.
2. Send only the three affected tasks for regeneration.
3. Use only the already accepted AWS source URLs.
4. Preserve every unaffected task exactly.
5. Save the alignment decision and pending-source list in the local Stage 6
   output.
6. Keep Stage 7 unprepared.

The two pending source candidates cannot be used until the human explicitly
approves them in a later controlled step.

8. SAFETY CHECKS

The revision save rejects:

1. A different service or session.
2. Unknown or duplicated task IDs.
3. Changes to unaffected Stage 6 tasks.
4. Changes to the Author, Supabase, AWS or Stage 7 boundaries.
5. Source evidence outside an accepted task boundary.
6. A pending source being treated as accepted.

9. FILES ADDED OR UPDATED

scripts/author-assistant/alignments/stage84a-sqs-console-boundary.json
scripts/author-assistant/authorAssistantStage84A.mjs
scripts/author-assistant/authorAssistantInstructions.mjs
scripts/author-assistant/authorAssistantCore.mjs
scripts/author-assistant/authorAssistant.mjs
tests/authorAssistantStage84A.test.js

10. VERIFICATION

Focused Step 84 and Step 84A tests:
16 passed, 0 failed.

Full application tests:
582 passed, 0 failed.

Lint:
0 errors. Existing unrelated warnings remain.

Production build:
Passed. The existing Node version and large-bundle warnings remain.

11. LIVE SESSION STATUS

The Step 84A implementation is ready.

The saved SQS Stage 6 file has not yet been regenerated because the live
request requires the user's private OpenAI API key. Tests never use or request
that key and never overwrite the user's saved session.

Nothing was written to Author, Supabase or AWS.
Stage 7 was not started.

