import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';

export default createAwsKnowledgeGuide({ id: 'aurora-24', topicId: 'topic-aurora', topicTitle: 'Amazon Aurora', objectiveCode: 'Databases', title: 'Aurora Replica Auto Scaling', status: 'ready', plainEnglish: 'Aurora Auto Scaling automatically adds or removes Aurora Replicas in response to a target-tracking policy. The policy observes an aggregated reader metric, such as average CPU utilization or average database connections, and adjusts replica count between configured minimum and maximum limits.', whyItMatters: 'Read demand can vary by time and event. Automatically changing reader capacity can keep read queries responsive while avoiding a permanently high replica count during quiet periods.', workplaceExample: 'An ecommerce service sees large read traffic during campaigns. It configures a target-tracking policy using reader average CPU utilization, a minimum reader baseline for availability, and a maximum that protects cost. Auto Scaling adds readers when the aggregate target is exceeded and removes them after demand falls.', examFocus: 'Auto Scaling changes the number of Aurora Replicas; it does not resize a provisioned DB instance or replace Serverless v2 ACU scaling. It works from the reader-side aggregate metrics RDSReaderAverageCPUUtilization or RDSReaderAverageDatabaseConnections. Set meaningful minimum and maximum replica limits, and ensure applications use the reader endpoint to benefit from added replicas.', keyPoints: ['Aurora Auto Scaling adds and removes Aurora Replicas.', 'Target tracking uses aggregated reader metrics.', 'Supported target metrics include reader average CPU and database connections.', 'Minimum and maximum replica counts are policy guardrails.', 'The reader endpoint distributes eligible read connections across replicas.'], commonMistake: 'Configuring Auto Scaling but sending all reads to the writer endpoint produces little benefit. Separate reads safely in the application, use the reader endpoint where appropriate, and test behavior during replica creation and removal.', example: 'Configure a test policy with a conservative reader-count range, then apply controlled read load. Expect the policy to evaluate reader aggregate load and eventually change replica count within the allowed range. Verify the reader endpoint, scaling activity history, read latency, and cost after the test.', sources: [{ title: 'Using Aurora Auto Scaling with Aurora Replicas', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Aurora.Integrating.AutoScaling.html' }, { title: 'Adding Aurora Auto Scaling', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Aurora.Integrating.AutoScaling.Add.html' }, { title: 'Amazon Aurora endpoints', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Aurora.Overview.Endpoints.html' }] });
