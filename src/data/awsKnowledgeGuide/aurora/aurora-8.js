import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';

export default createAwsKnowledgeGuide({
  id: 'aurora-8', topicId: 'topic-aurora', topicTitle: 'Amazon Aurora', objectiveCode: 'Databases', title: 'Aurora Multi-AZ Architecture', status: 'ready',
  plainEnglish: 'Aurora keeps its cluster volume across multiple Availability Zones in a Region. For compute high availability, place the writer and one or more reader instances in different AZs. If the writer becomes unavailable, Aurora can promote a reader in another AZ. This is different from simply having durable storage: an available reader instance provides ready compute capacity for a faster failover.',
  whyItMatters: 'Multi-AZ design reduces the impact of an instance or AZ problem. It gives applications a failover target and enables reads to continue through reader endpoints while write availability is restored.',
  workplaceExample: 'A payment service runs a writer in one AZ and two readers in separate AZs. An AZ issue affects the writer; Aurora promotes an eligible reader, and the service reconnects using the cluster endpoint rather than changing application configuration.',
  examFocus: 'For high availability, use at least one reader in a different AZ and connect through cluster or reader endpoints. Aurora storage is already replicated across AZs even with one instance, but a single-instance cluster has no existing reader to promote and must recreate a writer if it fails. Multi-AZ within one Region does not protect against a whole-Region outage; use Aurora Global Database for that objective.',
  keyPoints: ['Aurora storage copies span multiple AZs in one Region.', 'Reader instances in separate AZs provide compute failover capacity.', 'The cluster endpoint follows the current writer after failover.', 'The reader endpoint supports read connections across reader instances.', 'Global Database is the cross-Region disaster-recovery pattern.'],
  commonMistake: 'Calling a single-instance Aurora cluster fully compute Multi-AZ because the storage is distributed overlooks the writer failover path. Add readers in separate AZs for rapid promotion and test the application’s retry behavior.',
  example: 'Place a writer and reader in different AZs, connect through the cluster endpoint, and perform a controlled failover during a maintenance window. Expect a short interruption and reconnection to the promoted writer; verify the DB event history, endpoint resolution, and application recovery.',
  sources: [{ title: 'High availability for Amazon Aurora', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Concepts.AuroraHighAvailability.html' }, { title: 'Aurora DB clusters', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Aurora.Overview.html' }, { title: 'Aurora endpoint connections', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Aurora.Overview.Endpoints.html' }]
});
