import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';

export default createAwsKnowledgeGuide({
  id: 'aurora-20', topicId: 'topic-aurora', topicTitle: 'Amazon Aurora', objectiveCode: 'Databases', title: 'Aurora Backtrack for Aurora MySQL', status: 'ready',
  plainEnglish: 'Aurora Backtrack is an Aurora MySQL-Compatible feature that rewinds an entire DB cluster in place to an earlier consistent time within its backtrack window. It uses retained change records instead of creating a new cluster from backups. The target window is a goal; heavy workload can make the actual available window shorter. Backtrack pauses the cluster, closes connections, and discards uncommitted work during the operation.',
  whyItMatters: 'It can undo a recent destructive database mistake quickly, such as an accidental deletion, with less operational work than restoring a separate point-in-time cluster. It also supports investigation by moving backward and forward through changes.',
  workplaceExample: 'A developer accidentally deletes a table in a backtrack-enabled non-production Aurora MySQL cluster. The team pauses writers, backtracks to a consistent time before the deletion, verifies the table and data, and resumes the application.',
  examFocus: 'Backtrack is only for supported Aurora MySQL versions and must be enabled when the cluster is created or restored; it cannot be turned on later for an existing ordinary cluster. It affects the whole cluster, not one table, has a maximum backtrack window, and is not a replacement for backups. Choose PITR to create a separate recovered cluster.',
  keyPoints: ['Backtrack rewinds an entire Aurora MySQL cluster in place.', 'It uses change records within a target and actual backtrack window.', 'It returns the cluster to a consistent time near the requested point.', 'It interrupts connections and drops uncommitted reads and writes.', 'It is not a replacement for automated backups or snapshots.'],
  commonMistake: 'Backtracking production without stopping writes can discard active work and affect every database object. Pause or drain the application, confirm the actual backtrack window and target time, communicate the whole-cluster impact, and validate before resuming traffic.',
  example: 'In a non-production backtrack-enabled Aurora MySQL cluster, create a safe test row, record the time, remove the row, then backtrack to before removal. Expect the row to reappear after the cluster resumes. Verify the exact completed backtrack time and reconnect applications after the interruption.',
  sources: [{ title: 'Backtracking an Aurora DB cluster', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/AuroraMySQL.Managing.Backtrack.html' }, { title: 'Restoring Aurora to a specified time', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/aurora-pitr.html' }]
});
