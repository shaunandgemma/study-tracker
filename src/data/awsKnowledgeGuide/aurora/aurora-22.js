import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';

export default createAwsKnowledgeGuide({ id: 'aurora-22', topicId: 'topic-aurora', topicTitle: 'Amazon Aurora', objectiveCode: 'Databases', title: 'Aurora IAM Database Authentication', status: 'ready', plainEnglish: 'IAM database authentication lets an application or user connect to a supported Aurora database using a short-lived authentication token instead of a stored database password. IAM authorizes the identity to connect, while a database account still controls database permissions. The token is valid for 15 minutes and connections use SSL or TLS.', whyItMatters: 'It can reduce long-lived password handling and lets teams use IAM roles, temporary credentials, and centralized policies for connection authorization. It does not remove the need to design database users and least-privilege database grants.', workplaceExample: 'A workload running with an IAM role generates a short-lived token for its Aurora database account. Its IAM policy permits only rds-db:connect to that account, while the database grant limits the account to the application schema.', examFocus: 'IAM database authentication is optional and must be enabled on the cluster. Configure both layers: IAM permission such as rds-db:connect and a matching database user configured for IAM authentication. A token is not a reusable password; generate it near connection time and use the supported endpoint and TLS setup.', keyPoints: ['IAM database authentication uses short-lived connection tokens.', 'Tokens are valid for 15 minutes.', 'The cluster feature and matching database account must be configured.', 'IAM rds-db:connect permission authorizes the connection.', 'Database grants still govern what the connected account can do.'], commonMistake: 'Granting broad IAM access but forgetting database-user setup, or the reverse, causes failed connections. Test the exact IAM principal, database user, endpoint, TLS configuration, and token-generation flow together.', example: 'Give a non-production application role permission to connect only as one named database user, then connect with a newly generated token over TLS. Expect the connection to expire if a new token is not used after its validity period. Verify that the same role cannot connect as a more privileged database user.', sources: [{ title: 'IAM database authentication for Aurora', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/UsingWithRDS.IAMDBAuth.html' }, { title: 'Enabling IAM database authentication', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/UsingWithRDS.IAMDBAuth.Enabling.html' }, { title: 'Connecting with IAM database authentication', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/UsingWithRDS.IAMDBAuth.Connecting.html' }] });
