import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';

export default createAwsKnowledgeGuide({
  id: 'aurora-6', topicId: 'topic-aurora', topicTitle: 'Amazon Aurora', objectiveCode: 'Databases', title: 'Aurora DB Clusters', status: 'ready',
  plainEnglish: 'An Aurora DB cluster is the database unit that contains one or more DB instances and one shared cluster volume. It has one writer instance that handles writes and can also read, plus optional reader instances that handle read-only work. The shared volume stores the database data independently from the individual compute instances. Cluster settings include engine version, networking, backups, encryption, and endpoints.',
  whyItMatters: 'Architects design around the cluster rather than a single server. This lets them separate write, read, storage, backup, and availability decisions, add readers for capacity or failover, and connect applications through stable cluster endpoints.',
  workplaceExample: 'A retail system runs an Aurora cluster with one writer and two reader instances in different Availability Zones. Checkout uses the writer endpoint, reporting uses the reader endpoint, and the shared cluster volume holds the same database data for all instances.',
  examFocus: 'An Aurora cluster has exactly one writer and can have up to 15 Aurora Replicas. Readers share the cluster volume rather than receiving a full independent storage copy. Choose a cluster endpoint for write or general connections and a reader endpoint for read-only load distribution. Adding a reader is not the same as creating a separate cross-Region disaster-recovery database.',
  keyPoints: ['A DB cluster combines DB instances with a shared cluster volume.', 'One writer performs data modifications.', 'Reader instances process read-only requests and can be promoted.', 'Cluster-level settings govern storage, backup, encryption, and endpoints.', 'The shared storage layer is separate from DB instance compute.'],
  commonMistake: 'Connecting production applications to a specific instance endpoint makes failover handling fragile. Use cluster and reader endpoints for normal application traffic, reserving instance endpoints for exceptional operational needs.',
  example: 'Create a cluster with a writer and one reader in another Availability Zone. Connect a write test to the cluster endpoint and a read-only test to the reader endpoint. Verify that writes reach the writer, reads can be distributed, and the endpoints remain the recommended application connection targets.',
  sources: [{ title: 'Amazon Aurora DB clusters', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Aurora.Overview.html' }, { title: 'Creating an Aurora DB cluster', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Aurora.CreateInstance.html' }, { title: 'Aurora endpoint connections', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Aurora.Overview.Endpoints.html' }]
});
