import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';

export default createAwsKnowledgeGuide({
  id: 'aurora-10', topicId: 'topic-aurora', topicTitle: 'Amazon Aurora', objectiveCode: 'Databases', title: 'Aurora Automatic Failover', status: 'ready',
  plainEnglish: 'Automatic failover is Aurora’s process for replacing an unavailable writer with a working writer. When reader instances exist, Aurora normally promotes an eligible reader; without one, it recreates a writer. Promotion tiers set preferred reader order, with lower numbers preferred. During the transition, writes and some connections are interrupted, so applications must retry safely and reconnect through cluster-aware endpoints.',
  whyItMatters: 'Automatic failover reduces downtime from instance and AZ problems. A pre-existing reader is much faster to promote than creating new compute, and a stable cluster endpoint avoids a hard-coded server change.',
  workplaceExample: 'A writer fails during an incident. Aurora promotes the highest-priority suitable reader, the cluster endpoint redirects new connections, and application retry logic resumes database writes after the brief transition.',
  examFocus: 'For rapid Aurora writer failover, create one or more readers and distribute them across AZs. Use promotion tiers to guide selection and use the cluster endpoint rather than an instance endpoint. The reader endpoint keeps read capacity available, though it can briefly include the new writer after promotion. RDS Proxy can further reduce application disruption from DNS caching and connection handling.',
  keyPoints: ['Aurora detects writer failures and can fail over automatically.', 'A reader is promoted when one is available.', 'Promotion tiers influence preferred failover order.', 'Cluster and reader endpoints adapt to role changes.', 'Applications need retry and reconnection handling for the interruption.'],
  commonMistake: 'Believing an endpoint makes every in-flight transaction survive failover can result in data errors. Treat the event as a connection interruption, use idempotent retry patterns where appropriate, and make transaction handling explicit.',
  example: 'Set a suitable promotion tier for a reader in another AZ and run a controlled failover. Expect temporary connection errors followed by a new writer. Verify the DB events, writer role, cluster endpoint, and application retry metrics after the test.',
  sources: [{ title: 'High availability for Amazon Aurora', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Concepts.AuroraHighAvailability.html' }, { title: 'Aurora endpoint connections', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Aurora.Overview.Endpoints.html' }, { title: 'RDS Proxy concepts and terminology', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/rds-proxy.howitworks.html' }]
});
