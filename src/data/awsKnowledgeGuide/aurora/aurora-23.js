import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';

export default createAwsKnowledgeGuide({ id: 'aurora-23', topicId: 'topic-aurora', topicTitle: 'Amazon Aurora', objectiveCode: 'Databases', title: 'Aurora with RDS Proxy', status: 'ready', plainEnglish: 'Amazon RDS Proxy is a managed database proxy placed between an application and Aurora. It pools and reuses database connections so many application connections can share fewer database connections. The proxy can also help applications maintain connections through some database failovers, reducing connection-management work in the application.', whyItMatters: 'Opening many short-lived connections can consume database memory and connection limits. A proxy smooths that pressure and improves resilience for connection-heavy applications, especially serverless or bursty workloads.', workplaceExample: 'A Lambda-based service has many concurrent invocations. It connects to an RDS Proxy endpoint rather than directly to Aurora, allowing the proxy to pool connections while credentials are stored and retrieved through supported Secrets Manager or IAM integration.', examFocus: 'RDS Proxy manages connections; it is not a read-scaling replacement for Aurora replicas, a database cache, or a cross-Region disaster-recovery solution. Configure the proxy in the correct VPC and security model, authorize its access to the target database, and point clients at the proxy endpoint.', keyPoints: ['RDS Proxy sits between clients and Aurora.', 'It pools and multiplexes database connections.', 'It can reduce connection overhead on Aurora.', 'It can improve application behavior during failovers.', 'Clients use the proxy endpoint instead of the database endpoint.'], commonMistake: 'Assuming every session can be freely multiplexed ignores session pinning: some session state causes a client to stay pinned to one database connection. Monitor proxy metrics and reduce unnecessary session state before expecting maximum pooling efficiency.', example: 'Direct a connection-bursty test service to an RDS Proxy endpoint and compare its database connection count with the direct-connection baseline. Expect fewer backend connections when pooling is effective. Verify authentication, security groups, target health, and session-pinning metrics before production rollout.', sources: [{ title: 'Managing connections with Amazon RDS Proxy', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/rds-proxy.html' }, { title: 'How Amazon RDS Proxy works', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/rds-proxy.howitworks.html' }] });
