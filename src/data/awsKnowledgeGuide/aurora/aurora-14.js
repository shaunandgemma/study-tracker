import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';

export default createAwsKnowledgeGuide({
  id: 'aurora-14', topicId: 'topic-aurora', topicTitle: 'Amazon Aurora', objectiveCode: 'Databases', title: 'Aurora Storage Auto Scaling', status: 'ready',
  plainEnglish: 'Aurora cluster storage automatically grows as database data grows. The cluster volume is a distributed shared storage layer, so storage scaling is independent of selecting DB instance compute size. Aurora storage configuration and maximum volume capacity depend on the engine version; monitor volume metrics and choose the storage configuration that fits the workload and cost model.',
  whyItMatters: 'Automatic growth removes the need to preallocate a fixed storage size or schedule manual storage increases. It lets engineers focus on data growth monitoring, cost, and workload performance rather than capacity-change operations.',
  workplaceExample: 'A SaaS database receives unpredictable tenant growth. Its Aurora volume grows with the stored data while the team alarms on low remaining capacity indicators and reviews storage cost trends, avoiding a manual storage expansion outage.',
  examFocus: 'Aurora storage auto scaling means cluster storage grows automatically; it is not Aurora Replica Auto Scaling or Serverless v2 compute scaling. Compute still needs provisioned instance sizing or Serverless v2 capacity settings. Aurora Standard and Aurora I/O-Optimized are storage configurations with different pricing behavior, and availability depends on engine version and Region.',
  keyPoints: ['Aurora cluster volumes automatically grow with stored data.', 'Storage scaling is independent from DB instance compute scaling.', 'The cluster volume is shared by all DB instances.', 'Monitor storage metrics and costs as data grows.', 'Storage configuration availability depends on engine version and Region.'],
  commonMistake: 'Treating automatic growth as a reason not to monitor storage can miss maximum-volume limits, temporary local-storage pressure, or cost changes. Alarm on relevant Aurora volume metrics and distinguish persistent cluster storage from instance temporary storage.',
  example: 'Track VolumeBytesUsed and AuroraVolumeBytesLeftTotal for an active cluster. Expect used volume to grow as data is added. Verify the metrics use the correct DB cluster dimensions and investigate a falling remaining-capacity value before it threatens the workload.',
  sources: [{ title: 'Amazon Aurora storage', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Aurora.Overview.StorageReliability.html' }, { title: 'Supported storage configurations', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Concepts.Aurora_Fea_Regions_DB-eng.Feature.storage-type.html' }]
});
