import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';

export default createAwsKnowledgeGuide({ id: 'aurora-21', topicId: 'topic-aurora', topicTitle: 'Amazon Aurora', objectiveCode: 'Databases', title: 'Aurora Encryption with AWS KMS', status: 'ready', plainEnglish: 'Aurora encryption at rest protects the storage for a DB cluster using AWS Key Management Service (AWS KMS) keys. It encrypts the cluster volume, automated backups, snapshots, and logs. When creating a cluster, choose the appropriate supported key option. The encryption key for an existing encrypted cluster cannot be changed in place.', whyItMatters: 'Encryption helps meet security and compliance requirements without requiring the application to implement disk-level database encryption. Key permissions remain important because access to KMS is part of the recovery and operations design.', workplaceExample: 'A finance team creates an encrypted Aurora cluster using a customer managed KMS key so its key administrators can control policy and auditing. When it needs a different key, the team copies a snapshot with the intended key and restores a replacement cluster after testing.', examFocus: 'Aurora encryption at rest covers the underlying storage and backups. It is distinct from encryption in transit, which protects data moving between clients and the database. Plan the KMS key before creation because changing the key for an existing encrypted cluster requires a snapshot and restore workflow rather than an in-place switch.', keyPoints: ['Aurora encryption at rest uses AWS KMS.', 'Encrypted Aurora data includes storage, backups, snapshots, and logs.', 'Key choice is made when the encrypted cluster is created.', 'An existing encrypted cluster cannot change its KMS key in place.', 'Snapshot copy and restore provide a path to use a different key.'], commonMistake: 'Assuming database encryption removes the need to manage KMS access can break restores or operations. Review key policy, grants, service access, and cross-account or cross-Region backup procedures before an incident.', example: 'Create an encrypted test cluster with a designated KMS key, then create and restore a snapshot in the approved environment. Expect the restored data to remain encrypted. Verify authorized operators can use the key and unauthorized identities cannot access the database or key material.', sources: [{ title: 'Encrypting Amazon Aurora resources', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Overview.Encryption.html' }, { title: 'Managing KMS keys for Amazon Aurora encrypted resources', url: 'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Overview.Encryption.Keys.html' }] });
