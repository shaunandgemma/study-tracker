import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';

export default createAwsKnowledgeGuide({
  id: 'xray-1',
  topicId: 'topic-xray',
  topicTitle: 'AWS X-Ray',
  objectiveCode: 'Observability',
  title: 'AWS X-Ray Active Tracing & Daemon',
  status: 'ready',
  plainEnglish: 'AWS X-Ray is a distributed tracing service that collects and visualizes data about requests that travel through your multi-tier microservices and serverless architectures. As an HTTP or RPC request flows from an API Gateway or Load Balancer through Lambda functions, EC2 instances, ECS containers, and downstream databases (like Amazon DynamoDB or Amazon RDS), X-Ray assigns a unique Trace ID (`X-Amzn-Trace-Id`) to track the entire end-to-end journey. Individual components record timing and execution details as segments and subsegments, which are buffered and transmitted asynchronously by the X-Ray Daemon or AWS Distro for OpenTelemetry (ADOT) agent to the X-Ray service.',
  whyItMatters: 'In complex microservice and serverless environments, a single user request can trigger calls across dozens of decoupled services. When a request is slow or fails, traditional isolated server logs and aggregate metrics make it difficult to determine which specific downstream service caused the bottleneck. X-Ray pinpoints the exact component, database query, or third-party HTTP call causing high latency, faults (5xx), errors (4xx), or throttling (429).',
  workplaceExample: 'An e-commerce checkout service experiences intermittent 3-second response times during flash sales. The engineering team enables Active Tracing on Amazon API Gateway and AWS Lambda, and instruments the Node.js backend using the AWS X-Ray SDK. Inspecting traces in the X-Ray console reveals that a specific subsegment calling a third-party payment gateway is timing out and causing retries, allowing the team to add circuit breakers and timeout handling.',
  examFocus: 'SAA-C03 X-Ray Instrumentation & Architecture:\n- Traces & Trace IDs: A Trace maps the path of a request through an application. The `X-Amzn-Trace-Id` HTTP header propagates the trace context across service boundaries.\n- Segments vs Subsegments: A Segment provides resource data (hostname, request, response) generated by the host service. A Subsegment breaks down that segment to provide granular timing for downstream HTTP calls, AWS SDK calls, SQL database queries, and custom code blocks.\n- Annotations vs Metadata:\n  * Annotations: Key-value pairs indexed for use with X-Ray Filter Expressions and search queries (e.g., `userId`, `tier = "premium"`).\n  * Metadata: Key-value pairs with any data type (including objects and lists) that are NOT indexed and cannot be searched with filter expressions.\n- X-Ray Daemon: A lightweight background process listening on UDP port 2000 that batches trace segments and sends them via HTTPS (`xray:PutTraceSegments`, `xray:PutTelemetryRecords`) to the X-Ray backend. Deploy as an EC2 background agent, an ECS sidecar container, or an EKS DaemonSet.\n- Active Tracing in AWS Services: Enable with a single toggle in AWS Lambda (`TracingConfig: Mode=Active`), API Gateway, and AppSync without manual daemon installation.',
  keyPoints: [
    'Distributed tracing service that tracks user requests end-to-end across microservices and serverless applications.',
    'Generates a unique `X-Amzn-Trace-Id` header to propagate request context across all intermediate services.',
    'Segments represent the host service; Subsegments record detailed timing for downstream AWS SDK calls, SQL queries, and HTTP endpoints.',
    'Annotations are indexed key-value pairs used for filtering and search; Metadata is non-indexed contextual data.',
    'The X-Ray Daemon listens on UDP port 2000 to buffer and upload trace segments to the X-Ray service without blocking application threads.',
    'Lambda and API Gateway provide native Active Tracing toggles that handle daemon deployment automatically.'
  ],
  commonMistake: 'Storing high-cardinality search attributes in X-Ray Metadata instead of Annotations. Metadata is not indexed and cannot be queried using X-Ray filter expressions; always use Annotations for searchable fields.',
  example: 'Instrumenting AWS Services and Containers for X-Ray:\n- AWS Lambda: Set `TracingConfig: Mode=Active` in CloudFormation/SAM. AWS automatically runs the X-Ray daemon in the execution environment.\n- Amazon ECS Task: Add an X-Ray daemon sidecar container listening on UDP port 2000 (container image: `public.ecr.aws/xray/aws-xray-daemon:latest`).\n- IAM Permission: Attach the managed policy `AWSXRayDaemonWriteAccess` (`xray:PutTraceSegments`, `xray:PutTelemetryRecords`) to the execution/instance role.\n- Node.js SDK instrumentation: `const AWSXRay = require("aws-xray-sdk"); const AWS = AWSXRay.captureAWS(require("aws-sdk"));`',
  sources: [
    { title: 'What is AWS X-Ray?', url: 'https://docs.aws.amazon.com/xray/latest/devguide/aws-xray.html' },
    { title: 'AWS X-Ray daemon and architecture', url: 'https://docs.aws.amazon.com/xray/latest/devguide/xray-daemon.html' }
  ]
});
