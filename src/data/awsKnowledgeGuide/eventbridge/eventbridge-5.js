import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';

export default createAwsKnowledgeGuide({
  id: 'eventbridge-5',
  topicId: 'topic-eventbridge',
  topicTitle: 'Amazon EventBridge',
  objectiveCode: 'Integration',
  title: 'Default Event Bus',
  status: 'ready',
  plainEnglish: 'The Default Event Bus is a pre-configured event bus created automatically in every AWS account. It ingests system events generated by native AWS services (such as EC2 state changes, S3 bucket operations, GuardDuty security findings, or AWS CodePipeline notifications) as well as custom events sent using the `PutEvents` API without requiring any initial infrastructure setup.',
  whyItMatters: 'The Default Event Bus provides instant observability over your AWS account. You can immediately create EventBridge rules on the default bus to trigger automated reactions whenever AWS resources change state.',
  workplaceExample: 'An engineer creates an EventBridge rule on the Default Event Bus matching `source: aws.ec2` and `detail-type: EC2 Instance State-change Notification`. Whenever an EC2 instance is stopped or terminated in the account, the default event bus routes the event to an SNS topic that sends an SMS alert.',
  examFocus: 'SAA-C03 Default Event Bus characteristics:\n- Automatically created in every AWS region per account (named `default`).\n- Receives all native AWS service events automatically (EC2, S3, GuardDuty, Health, etc.).\n- Can also receive custom events via `PutEvents`, but custom event buses are recommended for application segregation.',
  keyPoints: [
    'Pre-existing event bus automatically provisioned in every AWS account and region.',
    'Receives all native AWS service system events automatically.',
    'Event bus name is fixed as `default`.',
    'Supports rules routing AWS system events to Lambda, SNS, SQS, or Step Functions.',
    'Can be extended with Resource Policies for cross-account event reception.'
  ],
  commonMistake: 'Attempting to delete or rename the Default Event Bus. The `default` event bus is built-in to AWS and cannot be deleted.',
  example: 'Matching an AWS Service Event on Default Bus:\n`{ "source": ["aws.ec2"], "detail-type": ["EC2 Instance State-change Notification"], "detail": { "state": ["stopped"] } }`',
  sources: [
    { title: 'Amazon EventBridge Event Buses', url: 'https://docs.aws.amazon.com/eventbridge/latest/userguide/eb-event-bus.html' }
  ]
});
