import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';

export default createAwsKnowledgeGuide({
  id: 'iam-25',
  topicId: 'topic-iam',
  topicTitle: 'AWS IAM (Identity and Access Management)',
  objectiveCode: 'Security',
  title: 'Multi-Factor Authentication - MFA',
  status: 'ready',
  plainEnglish: 'Multi-Factor Authentication (MFA) adds a vital layer of security on top of standard username and password login credentials. When MFA is enabled, logging into the AWS Management Console or calling sensitive APIs requires entering a unique authentication code generated by an approved physical hardware security key (FIDO2/WebAuthn YubiKey) or a virtual authenticator application (Google Authenticator, Authy).',
  whyItMatters: 'Passwords can be stolen via phishing, data breaches, or credential stuffing attacks. Enforcing MFA ensures that an attacker possessing a stolen password still cannot access your AWS account without physical possession of the MFA token.',
  workplaceExample: 'An enterprise enforces MFA across all IAM Console users. If an employee tries to access sensitive production S3 buckets or EC2 instances without logging in via MFA, an explicit IAM policy statement (`"Condition": {"Bool": {"aws:MultiFactorAuthPresent": "false"}}`) blocks all API requests.',
  examFocus: 'SAA-C03 MFA Best Practices & Policy Keys:\n- Mandate MFA for the AWS Account Root User and all human IAM console users.\n- Supported MFA Types: Hardware TOTP/FIDO2 security keys (YubiKey), Virtual MFA apps (Authenticator), and SMS (not recommended for high security).\n- Policy Key: `aws:MultiFactorAuthPresent` (evaluates whether the session was authenticated using MFA).\n- Enforce MFA for sensitive API actions like deleting S3 buckets or terminating production DBs.',
  keyPoints: [
    'Adds a mandatory second authentication factor to user sign-in.',
    'Requires physical token or virtual app code (FIDO2/YubiKey, Virtual TOTP).',
    'Mandatory best practice for the AWS Account Root User and all console users.',
    'Condition Key `aws:MultiFactorAuthPresent` enforces MFA for API actions.',
    'Prevents account takeover from stolen or leaked passwords.'
  ],
  commonMistake: 'Relying solely on passwords for IAM users with administrative access without enforcing MFA, exposing the AWS account to credential phishing attacks.',
  example: 'Sample MFA Enforcement Policy Statement JSON:\n{\n  "Version": "2012-10-17",\n  "Statement": [{\n    "Sid": "BlockNonMFAAccess",\n    "Effect": "Deny",\n    "Action": "s3:DeleteBucket",\n    "Resource": "*",\n    "Condition": {\n      "Bool": { "aws:MultiFactorAuthPresent": "false" }\n    }\n  }]\n}',
  sources: [
    { title: 'Using multi-factor authentication (MFA) in AWS', url: 'https://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_mfa.html' }
  ]
});
