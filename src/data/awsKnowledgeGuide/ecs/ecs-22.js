import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';
export default createAwsKnowledgeGuide({id:'ecs-22',topicId:'topic-ecs',topicTitle:'ECS (Elastic Container Service)',objectiveCode:'Containers',title:'ECS Task IAM Role',status:'ready',plainEnglish:'The task IAM role supplies temporary AWS credentials to application containers so their code can call AWS services.',whyItMatters:'Each workload receives only its required permissions instead of inheriting broad permissions from an EC2 host.',workplaceExample:'An order-processing task can read one SQS queue and write to one DynamoDB table through its task role.',examFocus:'Application permissions belong in taskRoleArn; permissions ECS needs to start the task belong in executionRoleArn.',keyPoints:['Credentials are delivered to containers automatically.','Use least-privilege policies.','The trust policy allows ecs-tasks.amazonaws.com.','CloudTrail can associate calls with task credentials.','Task roles separate workloads on the same cluster.'],commonMistake:'Giving application permissions only to the EC2 instance role or task execution role.',example:'Grant a worker task role sqs:ReceiveMessage and sqs:DeleteMessage on its queue.',sources:[{title:'Amazon ECS task IAM role',url:'https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task-iam-roles.html'}]});
