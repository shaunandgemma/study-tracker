import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';
export default createAwsKnowledgeGuide({id:'ecs-25',topicId:'topic-ecs',topicTitle:'ECS (Elastic Container Service)',objectiveCode:'Containers',title:'ECS with Application Load Balancer',status:'ready',plainEnglish:'An Application Load Balancer routes HTTP or HTTPS requests to healthy ECS tasks and can choose targets by host name or URL path.',whyItMatters:'It supplies a stable entry point while tasks are replaced, scaled and given changing IP addresses.',workplaceExample:'One ALB routes /orders to an orders ECS service and /catalogue to a catalogue service.',examFocus:'ALB is the usual ECS choice for Layer 7 routing; awsvpc tasks require an IP target group.',keyPoints:['ALB operates at Layer 7.','Health checks determine eligible targets.','Dynamic host-port mapping is supported.','Each service should use its own target group.','Security groups can restrict tasks to ALB traffic.'],commonMistake:'Allowing task ingress from the internet instead of only from the ALB security group.',example:'An HTTPS listener terminates TLS and forwards /api/* to healthy tasks on port 8080.',sources:[{title:'Use an Application Load Balancer for Amazon ECS',url:'https://docs.aws.amazon.com/AmazonECS/latest/developerguide/alb.html'}]});
