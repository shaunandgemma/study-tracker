import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';
export default createAwsKnowledgeGuide({id:'ecs-23',topicId:'topic-ecs',topicTitle:'ECS (Elastic Container Service)',objectiveCode:'Containers',title:'ECS Task Execution IAM Role',status:'ready',plainEnglish:'The task execution role lets the ECS and Fargate agents perform startup operations such as pulling a private ECR image, publishing logs and retrieving referenced secrets.',whyItMatters:'A task can fail before its application starts when the execution role lacks a required startup permission.',workplaceExample:'Fargate uses the execution role to pull an ECR image and create an awslogs stream.',examFocus:'Execution role is for ECS startup operations; task role is for application code.',keyPoints:['It is referenced by executionRoleArn.','The trust principal is ecs-tasks.amazonaws.com.','Common permissions cover ECR image pulls and CloudWatch Logs.','Extra permissions may be needed for secrets or private registry credentials.','It is not automatically the application role.'],commonMistake:'Adding DynamoDB application access to the execution role and expecting the container SDK to use it.',example:'Attach the managed execution-role policy, then add scoped Secrets Manager access only if the task definition references a secret.',sources:[{title:'Amazon ECS task execution IAM role',url:'https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task_execution_IAM_role.html'}]});
