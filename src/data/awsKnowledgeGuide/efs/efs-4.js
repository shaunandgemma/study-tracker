import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';

export default createAwsKnowledgeGuide({ id: 'efs-4', topicId: 'topic-efs', topicTitle: 'Amazon EFS (Elastic File System)', objectiveCode: 'Storage', title: 'EFS Replication (Cross-Region & Intra-Region Automated Replication)', status: 'ready', plainEnglish: 'EFS replication automatically copies a source Regional EFS file system to a separate destination file system in another Region or, where supported, within the same Region. Replication is asynchronous: the destination is a recovery or regional copy, not the same shared writable file system.', whyItMatters: 'It provides a managed data-copy path for disaster recovery and regional resilience without creating a custom synchronization process.', workplaceExample: 'A team replicates production shared files to a recovery Region, documents how applications will mount the destination, and tests its failover procedure during a resilience exercise.', examFocus: 'Replication creates a distinct destination file system and has replication lag. It is not active-active multi-writer storage. The destination has encryption at rest enabled, but recovery still requires network, mount-target, client-routing, and application planning.', keyPoints: ['Replication copies a source EFS file system to a separate destination.', 'It supports cross-Region and supported intra-Region designs.', 'Replication is asynchronous.', 'The destination is a recovery copy, not shared active-active storage.', 'Recovery requires application and mount planning.'], commonMistake: 'Treating the destination as an always-current writable peer can cause an invalid recovery design. Keep write ownership and failover steps explicit.', example: 'Create a non-production replication configuration, create test files on the source, and verify they arrive at the destination. Confirm the recovery runbook identifies the destination mount targets and the application cutover step.', sources: [{ title: 'Configuring replication to a new EFS file system', url: 'https://docs.aws.amazon.com/efs/latest/ug/create-replication.html' }, { title: 'Features of Amazon EFS', url: 'https://docs.aws.amazon.com/efs/latest/ug/features.html' }] });
