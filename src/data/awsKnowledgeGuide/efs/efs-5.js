import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';

export default createAwsKnowledgeGuide({ id: 'efs-5', topicId: 'topic-efs', topicTitle: 'Amazon EFS (Elastic File System)', objectiveCode: 'Storage', title: 'EFS Managed NFS File Storage', status: 'ready', plainEnglish: 'Amazon Elastic File System (Amazon EFS) is a fully managed shared file system that uses the Network File System (NFS) protocol. Linux-based AWS compute can mount the same EFS file system and see one shared directory tree. AWS manages the underlying file servers, storage scaling, and durability design.', whyItMatters: 'It removes the need to build and operate a self-managed NFS server when several workloads need the same files.', workplaceExample: 'A fleet of EC2 web servers mounts EFS for customer uploads. Any server can read a file uploaded through another server, so the application does not need to copy files between instances.', examFocus: 'Choose EFS for shared Linux file access over NFS. Contrast it with EBS, which is block storage usually attached to one EC2 instance, and S3, which is object storage accessed through APIs.', keyPoints: ['EFS is fully managed shared file storage.', 'Clients access EFS using NFS.', 'Multiple compute instances can mount one file system.', 'EFS presents a shared directory hierarchy.', 'AWS manages underlying storage infrastructure.'], commonMistake: 'Treating EFS like a local disk can hide network latency and shared-write coordination needs. Design applications for a network file system.', example: 'Mount a non-production EFS file system on two EC2 instances, create a test file from the first, and confirm it is visible from the second. Verify POSIX ownership and permissions match the workload design.', sources: [{ title: 'Features of Amazon EFS', url: 'https://docs.aws.amazon.com/efs/latest/ug/features.html' }, { title: 'Amazon EFS implementation summary', url: 'https://docs.aws.amazon.com/efs/latest/ug/how-it-works-implementation.html' }] });
