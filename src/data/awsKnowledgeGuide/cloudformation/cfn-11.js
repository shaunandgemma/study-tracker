import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';

export default createAwsKnowledgeGuide({
  "id": "cfn-11",
  "topicId": "topic-cloudformation",
  "topicTitle": "AWS CloudFormation",
  "objectiveCode": "Management",
  "title": "Resources",
  "status": "ready",
  "plainEnglish": "The Resources section is the heart of every CloudFormation template. It declares the actual AWS components—such as EC2 instances, S3 buckets, VPC subnets, and IAM roles—that you want CloudFormation to create and manage in your AWS account. Each resource block requires a logical name chosen by you, an official AWS resource Type (such as AWS::EC2::VPC), and a Properties block specifying the configuration details for that resource.",
  "whyItMatters": "The Resources section translates business requirements into actual cloud infrastructure. Without resource declarations, a template is empty and cannot perform any deployment or infrastructure management actions in your account.",
  "workplaceExample": "An application architecture requires an isolated virtual network. The engineer adds a resource block named MainVPC with type AWS::EC2::VPC and properties defining the CIDR block 10.0.0.0/16, DNS resolution flags, and environment tags.",
  "examFocus": "For SAA-C03, remember that Resources is the only mandatory section in a template. Understand resource syntax: Type specifies the AWS resource type (formatted as AWS::Service::Resource), and Properties defines resource options. Know that CloudFormation assigns physical IDs to resources upon creation based on the stack name and logical ID.",
  "keyPoints": [
    "Resources is the mandatory top-level section that defines all infrastructure components.",
    "Every resource entry requires a logical name, a Type identifier, and a Properties block.",
    "Resource type names follow the AWS::Service::ResourceType naming convention (e.g., AWS::S3::Bucket).",
    "Logical names are used within the template to reference resources via !Ref and !GetAtt.",
    "Physical IDs are generated by CloudFormation during stack execution to uniquely identify actual AWS objects."
  ],
  "commonMistake": "Misspelling the official AWS resource Type or using invalid property names, which causes CloudFormation stack creation to fail immediately during template syntax validation. Always reference the official AWS CloudFormation Resource Specification for accurate type and property names.",
  "example": "AWSTemplateFormatVersion: '2010-09-09'\nDescription: Defines core VPC and S3 bucket resources.\nResources:\n  ProductionVPC:\n    Type: AWS::EC2::VPC\n    Properties:\n      CidrBlock: 10.0.0.0/16\n      EnableDnsHostnames: true\n      Tags:\n        - Key: Name\n          Value: ProductionVPC",
  "sources": [
    {
      "title": "Resources Section Structure",
      "url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/resources-section-structure.html"
    },
    {
      "title": "AWS Resource and Property Types Reference",
      "url": "https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-template-resource-type-ref.html"
    }
  ]
});
