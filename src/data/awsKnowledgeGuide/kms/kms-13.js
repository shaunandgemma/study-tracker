import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';

export default createAwsKnowledgeGuide({
  id: 'kms-13',
  topicId: 'topic-kms',
  topicTitle: 'AWS KMS (Key Management Service)',
  objectiveCode: 'Security',
  title: 'Data Encryption Keys',
  status: 'ready',
  plainEnglish: 'A Data Encryption Key (DEK) is a symmetric encryption key generated by AWS KMS (via the `GenerateDataKey` API) that your application uses directly to encrypt large datasets, files, or database records locally in memory. Unlike KMS keys (which are managed by KMS and never leave KMS HSMs), Data Encryption Keys are returned to your application in plaintext so you can perform local encryption.',
  whyItMatters: 'Direct KMS API encryption cannot handle large files due to the 4 KB payload limit. Data Encryption Keys enable local encryption of gigabyte-sized files without payload restrictions, while keeping the DEK itself secured under a root KMS key.',
  workplaceExample: 'A database application calls `GenerateDataKey(KeyId="alias/db-key", KeySpec="AES_256")`. KMS returns a 256-bit plaintext DEK and an encrypted copy of the DEK. The app encrypts database records with the plaintext DEK, saves the encrypted DEK in the database header, and zeroes out the plaintext DEK in memory.',
  examFocus: 'SAA-C03 Data Encryption Key (DEK) Rules:\n- Generation: Created using the `GenerateDataKey` or `GenerateDataKeyWithoutPlaintext` API.\n- Plaintext DEK: Returned to application memory for local data encryption; MUST be purged from memory immediately after use.\n- Encrypted DEK: Stored alongside the encrypted data (ciphertext).\n- Decryption: To decrypt data, send the encrypted DEK to KMS `Decrypt` API to retrieve the plaintext DEK.',
  keyPoints: [
    'Symmetric key used directly by applications to encrypt data locally.',
    'Generated by calling the KMS `GenerateDataKey` API under a root KMS key.',
    'Plaintext DEK is returned for local encryption and must be purged from memory after use.',
    'Encrypted DEK copy is stored alongside the encrypted payload.',
    'Bypasses the 4 KB payload limit of direct KMS `Encrypt` API calls.'
  ],
  commonMistake: 'Storing plaintext Data Encryption Keys in application configuration files or persistent disks, bypassing envelope security.',
  example: 'Generating a Data Key via AWS CLI:\naws kms generate-data-key --key-id <KMS_KEY_ID> --key-spec AES_256',
  sources: [
    { title: 'Data keys', url: 'https://docs.aws.amazon.com/kms/latest/developerguide/concepts.html#data-keys' }
  ]
});
