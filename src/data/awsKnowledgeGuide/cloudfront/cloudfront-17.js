import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';

export default createAwsKnowledgeGuide({
  id: 'cloudfront-17',
  topicId: 'topic-cloudfront',
  topicTitle: 'Amazon CloudFront',
  objectiveCode: 'Networking',
  title: 'CloudFront Signed URLs',
  status: 'ready',
  plainEnglish: 'A CloudFront Signed URL is a URL that includes cryptographic authentication information (such as an expiration time, IP address restrictions, and a signature) allowing a user temporary access to specific private content. Signed URLs are generated by an application server using a public/private key pair (or CloudFront Key Group). When a user requests a Signed URL, CloudFront verifies the signature and expiration before serving the content.',
  whyItMatters: 'Signed URLs allow web applications to serve premium, private, or paywalled content (such as paid video streams, downloadable software, or private reports) securely over a global CDN without making the underlying storage bucket public.',
  workplaceExample: 'An online learning platform sells individual video courses. When a student purchases a course, the web backend generates a CloudFront Signed URL valid for 2 hours for that specific video file (https://cdn.example.com/course1/video.mp4?Expires=...&Signature=...). The student can stream the video, but cannot share the link permanently with non-subscribers.',
  examFocus: 'SAA-C03 rule of thumb:\n- Use Signed URLs when you want to restrict access to individual files (e.g. one software download).\n- Use Signed URLs if your clients don\'t support HTTP cookies (e.g. custom mobile apps or IoT devices).\n- Contrast with S3 Presigned URLs: S3 Presigned URLs fetch directly from S3 bypassing CloudFront caching; CloudFront Signed URLs fetch through CloudFront edge locations, leveraging CDN caching and DDoS protection.',
  keyPoints: [
    'Grants access to individual files or paths for a limited time.',
    'Requires a CloudFront Public Key Group or legacy CloudFront trusted key pair.',
    'Best when users use web clients/devices that do not support HTTP cookies.',
    'Supports IP address restrictions and start/expiration timestamps.',
    'Differs from S3 Presigned URLs by routing traffic through CloudFront edge locations.'
  ],
  commonMistake: 'Confusing CloudFront Signed URLs with S3 Presigned URLs. S3 Presigned URLs bypass CloudFront and hit S3 directly (losing CDN caching). CloudFront Signed URLs use CloudFront edge locations.',
  example: 'CloudFront Signed URL Structure:\n`https://d111111abcdef8.cloudfront.net/content/lesson1.mp4?Expires=1700000000&Signature=a1b2c3...&Key-Pair-Id=K2JC7ABCDEFG`',
  sources: [
    { title: 'Using signed URLs', url: 'https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/private-content-signed-urls.html' }
  ]
});
