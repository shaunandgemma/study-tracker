import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';

export default createAwsKnowledgeGuide({ id: 'cognito-16', topicId: 'topic-cognito', topicTitle: 'Amazon Cognito', objectiveCode: 'Security', title: 'Identity Pool IAM Roles', status: 'ready', plainEnglish: 'Identity-pool IAM roles are the roles that application users assume when Cognito gives them temporary AWS credentials. The role trust policy permits the Cognito identity service to assume the role under appropriate conditions, while the role permission policy states which AWS actions and resources the user can access.', whyItMatters: 'This is the authorization boundary for direct client access to AWS. A careful role design limits the blast radius of compromised client devices or tokens.', workplaceExample: 'An identity pool maps authenticated users to a role that can put objects only in a user-specific S3 location. A separate guest role has no write permission and can read only public catalog data.', examFocus: 'A trust policy answers who can assume a role; a permissions policy answers what the role can do. Identity pools can use separate authenticated and unauthenticated roles. Scope the trust policy to the intended identity pool and scope permissions to the smallest useful actions and resources.', keyPoints: ['Identity-pool users assume IAM roles for temporary credentials.', 'Trust policies control who can assume the role.', 'Permission policies control allowed AWS actions.', 'Authenticated and guest users can receive different roles.', 'Least privilege should include resource-level restrictions where possible.'], commonMistake: 'Attaching a broad managed policy to a client-facing role gives every app user excessive authority. Use narrowly scoped policies and test both expected success and denial paths.', example: 'Attach a test authenticated role that permits one S3 action on one test prefix, then exchange a test identity for credentials. Verify the allowed action succeeds while another bucket or action is denied.', sources: [{ title: 'Identity pools console overview', url: 'https://docs.aws.amazon.com/cognito/latest/developerguide/identity-pools.html' }, { title: 'Getting credentials', url: 'https://docs.aws.amazon.com/cognito/latest/developerguide/getting-credentials.html' }] });
