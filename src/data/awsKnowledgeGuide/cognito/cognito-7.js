import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';

export default createAwsKnowledgeGuide({ id: 'cognito-7', topicId: 'topic-cognito', topicTitle: 'Amazon Cognito', objectiveCode: 'Security', title: 'Identity Pools for Temporary AWS Credentials', status: 'ready', plainEnglish: 'An identity pool obtains a Cognito identity for an app user and exchanges it for short-lived AWS credentials through web identity federation. The credentials are governed by the IAM role assigned to the identity, rather than by long-term credentials embedded in the app.', whyItMatters: 'Temporary credentials reduce secret exposure and make direct client access to approved AWS services possible with an expiry and a clear IAM policy boundary.', workplaceExample: 'A mobile app exchanges a signed-in identity for credentials that permit a limited DynamoDB action set for its own records. The credentials expire, so the app refreshes them through the supported SDK flow.', examFocus: 'A Cognito identity is an identifier, not AWS credentials. The identity pool exchanges it for temporary credentials. Use authenticated roles after provider verification, and use unauthenticated roles only when limited guest access is intentionally required.', keyPoints: ['Identity pools issue temporary, limited-privilege AWS credentials.', 'Cognito identities are exchanged for credentials through federation.', 'IAM roles define the allowed AWS actions.', 'Credentials expire and must be refreshed by the supported flow.', 'Do not embed permanent AWS access keys in client apps.'], commonMistake: 'Confusing an ID token with AWS credentials causes failed or insecure integrations. Tokens prove app authentication; the identity pool role credentials authorize AWS API calls.', example: 'Sign in to a test app and use its identity pool integration to retrieve credentials. Expect temporary credentials that work only for the attached role policy. Verify a request outside that policy receives an access-denied response.', sources: [{ title: 'Getting credentials', url: 'https://docs.aws.amazon.com/cognito/latest/developerguide/getting-credentials.html' }, { title: 'Identity pools console overview', url: 'https://docs.aws.amazon.com/cognito/latest/developerguide/identity-pools.html' }] });
