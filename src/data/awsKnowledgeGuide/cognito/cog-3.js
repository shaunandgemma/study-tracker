import { createAwsKnowledgeGuide } from '../createAwsKnowledgeGuide.js';

export default createAwsKnowledgeGuide({ id: 'cog-3', topicId: 'topic-cognito', topicTitle: 'Amazon Cognito', objectiveCode: 'Security', title: 'Cognito Lambda Triggers (Pre-sign up, Post-confirmation, Custom Message customization)', status: 'ready', plainEnglish: 'User pool Lambda triggers let Amazon Cognito invoke your Lambda function at defined points in a user lifecycle. A pre sign-up trigger can accept or deny a registration after custom checks. A post-confirmation trigger runs after successful confirmation for follow-up work. A custom message trigger can tailor sign-up, verification, and password-reset messages.', whyItMatters: 'Triggers add controlled business rules to managed authentication without moving password storage or token issuance into custom code.', workplaceExample: 'A customer portal uses pre sign-up to block registrations from an unapproved domain, post-confirmation to create a profile record, and custom message to localize verification text.', examFocus: 'Triggers receive an event and usually return a response. Keep them fast, idempotent, and least-privileged because they sit on the sign-up or authentication path. Pre sign-up controls whether registration continues; post-confirmation is for downstream actions after it succeeds; custom message changes supported notification content.', keyPoints: ['Lambda triggers customize user-pool workflows.', 'Pre sign-up can accept or deny a registration.', 'Post-confirmation runs after user confirmation.', 'Custom message changes supported Cognito messages.', 'Trigger functions need appropriate Lambda permissions and failure handling.'], commonMistake: 'Putting slow, nonessential work in a synchronous trigger can make sign-up unreliable. Keep the trigger focused and send longer downstream work to an asynchronous service.', example: 'For a test user pool, configure a pre sign-up trigger that rejects a known test domain and a post-confirmation trigger that records a simple audit event. Verify the rejected user is not accepted and the confirmed user causes one downstream action.', sources: [{ title: 'Customizing user pool workflows with Lambda triggers', url: 'https://docs.aws.amazon.com/cognito/latest/developerguide/cognito-user-pools-working-with-lambda-triggers.html' }, { title: 'Authentication with Amazon Cognito user pools', url: 'https://docs.aws.amazon.com/cognito/latest/developerguide/authentication.html' }] });
